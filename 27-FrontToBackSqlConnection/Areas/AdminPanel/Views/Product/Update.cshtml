@model ProductUpdateVM

@{
    ProductImage mainImage = Model.ProductImages.FirstOrDefault(pi => pi.IsPrimary == true);
    ProductImage hoverImage = Model.ProductImages.FirstOrDefault(pi => pi.IsPrimary == false);
    List<ProductImage> addImages = Model.ProductImages.Where(pi => pi.IsPrimary == null).ToList();
}
<div class="row">
    <div class="col-md-6 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Update Product</h4>
               
                <form class="forms-sample" method="post" enctype="multipart/form-data">
                    <div class="form-group">
                        <div class="form-group">
                            <img style="width:100px" src="~/assets/images/website-images/@mainImage.Image" />
                        </div>
                        <label for="name">MainImage</label>
                        <input asp-for="MainPhoto" class="form-control" accept="image/*">
                        <span asp-validation-for="MainPhoto" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <div class="form-group">
                            <img style="width:100px" src="~/assets/images/website-images/@hoverImage.Image" />
                        </div>
                        <label for="name">SecondaryImage</label>
                        <input asp-for="HoverPhoto" class="form-control" accept="image/*">
                        <span asp-validation-for="HoverPhoto" class="text-danger"></span>
                    </div>
                    <div class="form-group d-flex flex-lg-wrap">
                        @foreach(ProductImage addimage in addImages)
                            {
                            <div style="position:relative">
                                <img style="width:100px" src="~/assets/images/website-images/@addimage.Image" />
                                <i class="custom-btn mdi mdi-close-circle" style="position:absolute; top:5px; right:5px; cursor:pointer"></i>
                                <input asp-for="ImageIds" type="hidden" value="@addimage.Id"/>
                            </div>
                            }
                            <label for="name">Additional Images</label>
                        <input asp-for="AdditionalPhotos" class="form-control" accept="image/*">
                        <span asp-validation-for="ImageIds" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label for="name">Name</label>
                        <input asp-for="Name" class="form-control" placeholder="Name">
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label for="name">Price</label>
                        <input asp-for="Price" class="form-control" placeholder="Price">
                        <span asp-validation-for="Price" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label for="name">SKU</label>
                        <textarea asp-for="SKU" class="form-control" placeholder="SKU"></textarea>
                        <span asp-validation-for="SKU" class="text-danger"></span>
                    </div>
                    
                    <div class="form-group">
                        <label for="name">Category</label>
                        <select class="form-control" asp-for="CategoryId" asp-items="new SelectList(Model.Categories,nameof(Category.Id),nameof(Category.Name))" >
                            <option selected disabled>Choose Category</option>
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label for="TagIds">Tag</label>
                        <select class="form-control" asp-for="TagIds" asp-items="new SelectList(Model.Tags, nameof(Tag.Id), nameof(Tag.Name))">
                            
                        </select>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label for="name">Description</label>
                        <input asp-for="Description" class="form-control" placeholder="Description">
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-primary me-2">Submit</button>
                    <a asp-action="Index" class="btn btn-light">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>

@section DeleteImages{
    <script>
        var btns=document.querySelectorAll(".custom-btn");
        for(let i=0;i<=btns.length;i++){
            btns[i].addEventListener("click",()=>{
                btns[i].parentElement.remove();
            })
        }
    </script>
}